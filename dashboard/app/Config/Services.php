<?php

namespace Config;

use CodeIgniter\Config\BaseService;

/**
 * Services Configuration file.
 *
 * Services are simple, static class methods that return an instance of a service,
 * or class, that is needed to run your application. This file defines any services
 * unique to this application.
 */
class Services extends BaseService
{
    /*
     * public static function someService($getShared = true)
     * {
     *     if ($getShared) {
     *         return static::getSharedInstance('someService');
     *     }
     *
     *     return new \App\Services\SomeService();
     * }
     */
}
